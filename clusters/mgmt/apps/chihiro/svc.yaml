apiVersion: v1
kind: Service
metadata:
  name: chihiro-service
  labels:
    app.kubernetes.io/name: chihiro-service
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      name: http
  selector:
    app.kubernetes.io/name: chihiro
