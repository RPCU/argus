apiVersion: vault.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: default
spec:
  address: http://vault.vault.svc:8200
  credentials:
    source: Secret
    secretRef:
      name: vault-creds
      namespace: crossplane-system
      key: credentials
