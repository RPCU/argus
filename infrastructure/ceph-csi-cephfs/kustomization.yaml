namespace: ceph-csi-cephfs
resources:
  - namespace.yaml
  - helmrepo.yaml
  - helmrelease.yaml
  - externalsecret.yaml
configMapGenerator:
  - name: ceph-csi-cephfs-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
