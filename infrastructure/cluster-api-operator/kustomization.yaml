namespace: capi-operator-system
resources:
  - namespace.yaml
  - helmrepo.yaml
  - helmrelease.yaml
configMapGenerator:
  - name: cluster-api-operator-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
