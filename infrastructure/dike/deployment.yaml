apiVersion: apps/v1
kind: Deployment
metadata:
  name: dike-controller-manager
  namespace: dike-system
  labels:
    app.kubernetes.io/name: dike
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: dike
  template:
    metadata:
      labels:
        app.kubernetes.io/name: dike
    spec:
      serviceAccountName: dike-controller-manager
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: manager
          image: zot.rpcu.io/public/dike:v0.7.0
          imagePullPolicy: IfNotPresent
          command:
            - /manager
          args:
            - --leader-elect
            - --health-probe-bind-address=:8081
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - "ALL"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            limits:
              cpu: 500m
              memory: 128Mi
            requests:
              cpu: 10m
              memory: 64Mi
      terminationGracePeriodSeconds: 10
