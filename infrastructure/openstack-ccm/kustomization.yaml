namespace: kube-system
resources:
  - namespace.yaml
  - helmrepo.yaml
  - helmrelease.yaml
configMapGenerator:
  - name: openstack-ccm-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
