apiVersion: batch/v1
kind: Job
metadata:
  name: cinder-ks-endpoints
spec:
  template:
    spec:
      initContainers:
        - name: ks-endpoints-public
          env:
            - name: OS_SERVICE_ENDPOINT
              value: "http://openstack.rpcu.vpn/cinder/v3"
