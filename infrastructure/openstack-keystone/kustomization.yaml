namespace: openstack
resources:
  - helmrelease.yaml
configMapGenerator:
  - name: keystone-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
patches:
  - path: patch-ks-endpoints-job.yaml
