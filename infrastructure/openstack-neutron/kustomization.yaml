namespace: openstack
resources:
  - helmrelease.yaml
  - cm-bin.yaml
configMapGenerator:
  - name: neutron-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
patches:
  - path: patch-ks-endpoints-job.yaml
