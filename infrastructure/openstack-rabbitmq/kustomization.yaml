namespace: openstack
resources:
  - namespace.yaml
  - helmrepo.yaml
  - helmrelease.yaml
configMapGenerator:
  - name: rabbitmq-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
